language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t teerminuz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true teerminuz/docker-react npm run test -- --coverage
